name: compile

on:
  push:
    branches: [ "main" ]
jobs:
  build:
    runs-on: Self-hosted
    steps:
    - uses: actions/checkout@v3
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet publish -r linux-x64 --no-self-contained -o /opt/www/mega
